[phases.install]
cmds = ["pnpm install --frozen-lockfile --prod=false"]

[phases.build]
cmds = ["pnpm --filter api build"]

[start]
cmd = "node apps/api/dist/db/migrate.js && node apps/api/dist/index.js"
